<h1>Toppages#index</h1>
<p>Find me in app/views/toppages/index.html.erb</p>

    <% if logged_in? %>
      <p>ログイン完了ユーザ: <%= current_user.name %></p>
      <%= link_to 'タスクを確認!', tasks_path, class: 'btn btn-lg btn-primary' %>
    <% else %>

    <div class="center jumbotron">
      <div class="text-center">
        <h1>Welcome to the Tasklist-App</h1>
            <%= link_to '新規登録!', signup_path, class: 'btn btn-lg btn-primary' %>　
            <%= link_to 'ログイン!', sessions_new_path, class: 'btn btn-lg btn-primary' %>
      </div>
    </div>
    <% end %>

